package net

const (
	// restAPIProtocol defines the communication protocol
	// identifier for the REST functions
	restAPIProtocol string = "http://"

	// usedContentType defines the content type used
	// by both the client and the server
	usedContentType string = "application/json"
)
